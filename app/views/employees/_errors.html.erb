<% if @employee.employee_model.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@employee.employee_model.errors.count, "error") %> prevented this Employee from being saved:</h2>

    <ul>
      <% @employee.employee_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>