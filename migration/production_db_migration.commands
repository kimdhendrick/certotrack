
-- rails console:
User.all.each do |user|
  user.roles = user.customer.roles
  user.username = user.username.downcase
  user.save(validate: false)
end

User.where(username: 'admin').first.update_attribute(:password, 'Password123')
-- rails console
